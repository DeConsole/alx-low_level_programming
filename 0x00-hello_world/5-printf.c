#include <stdio.h>
/**
 * main - Start of compilation
 *
 * Return: 0 null value returned after running
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
